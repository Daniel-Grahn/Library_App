package se.yrgo.integrations;

import org.openqa.selenium.WebDriver;

public class LendingStepDefinitions {
    private WebDriver driver;

    public LendingStepDefinitions(WebDriver driver) {
        this.driver = driver;
    }

    
}
